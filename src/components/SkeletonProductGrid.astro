---

---

<div class="container mx-auto px-4 py-8">
  <!-- Skeleton para el contador -->
  <div class="mb-6 text-center">
    <div class="h-5 w-48 bg-gray-300 rounded mx-auto animate-pulse"></div>
  </div>

  <!-- Skeleton para la grid de productos -->
  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
    {
      Array.from({ length: 6 }).map((_, index) => (
        <div class="bg-white rounded-lg shadow-md border border-gray-100 p-4">
          <div class="w-full h-64 bg-gray-300 rounded-md animate-pulse mb-4" />
          <div class="h-6 bg-gray-300 rounded animate-pulse mb-2" />
          <div class="h-4 bg-gray-300 rounded animate-pulse mb-2 w-3/4" />
          <div class="h-5 bg-gray-300 rounded animate-pulse w-1/3" />
        </div>
      ))
    }
  </div>

  <!-- Skeleton para la paginación -->
  <div class="flex justify-center mt-12">
    <div class="flex items-center space-x-2">
      <div class="h-10 w-20 bg-gray-300 rounded animate-pulse"></div>
      <div class="h-10 w-10 bg-gray-300 rounded animate-pulse"></div>
      <div class="h-10 w-10 bg-gray-300 rounded animate-pulse"></div>
      <div class="h-10 w-10 bg-gray-300 rounded animate-pulse"></div>
      <div class="h-10 w-20 bg-gray-300 rounded animate-pulse"></div>
    </div>
  </div>
</div>
